import { FooterHandlerDirective } from './footer-handler.directive';

describe('FooterHandlerDirective', () => {
  it('should create an instance', () => {
    const directive = new FooterHandlerDirective();
    expect(directive).toBeTruthy();
  });
});
